VERSION = 1,

option Injectors.GLOBAL_EFFECT_POLLING_RATE {
    type = integer,
    default = 60,
    min = 1,
    max = 1000,

    page = Injectors_global,
    translation = Injectors.GLOBAL_EFFECT_POLLING_RATE,
}

option Injectors.GLOBAL_OVERDOSE_DECAY {
    type = double,
    default = 0.0,
    min = 0.0,
    max = 100.0,

    page = Injectors_global,
    translation = Injectors.GLOBAL_OVERDOSE_DECAY,
}

option Injectors.GLOBAL_OVERDOSE_RATE {
    type = double,
    default = 0.0,
    min = 0.0,
    max = 600.0,

    page = Injectors_global,
    translation = Injectors.GLOBAL_OVERDOSE_RATE,
}

option Injectors.GLOBAL_PAINKILLERS_OVERDOSE_PENALTY {
    type = double,
    default = 0.0,
    min = 0.0,
    max = 100.0,

    page = Injectors_global,
    translation = Injectors.GLOBAL_PAINKILLERS_OVERDOSE_PENALTY,
}

option Injectors.GLOBAL_OVERDOSE_THRESHOLD {
    type = double,
    default = 100.0,
    min = 0.0,
    max = 1000.0,

    page = Injectors_global,
    translation = Injectors.GLOBAL_OVERDOSE_THRESHOLD,
}

option Injectors.GLOBAL_OVERDOSE_DEATH_ENABLED {
    type = boolean,
    default = true,

    page = Injectors_global,
    translation = Injectors.GLOBAL_OVERDOSE_DEATH_ENABLED,
}

option Injectors.GLOBAL_LOGGER_OFFSET {
    type = integer,
    default = 0,
    min = -24,
    max = 24,

    page = Injectors_global,
    translation = Injectors.GLOBAL_LOGGER_OFFSET,
}