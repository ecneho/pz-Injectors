module injectors {
    imports {
        Base
    }

    model injector_red-closed
    {
        mesh    = injectorclosed,
        texture = WorldItems/injector_red,
        scale   = 0.17,
    }

    model injector_blue-closed
    {
        mesh    = injectorclosed,
        texture = WorldItems/injector_blue,
        scale   = 0.17,
    }

    model injector_green-closed
    {
        mesh    = injectorclosed,
        texture = WorldItems/injector_green,
        scale   = 0.17,
    }

    model injector-empty
    {
        mesh    = injectorclosed,
        texture = WorldItems/empty,
        scale   = 0.17,
    }
}